import React from 'react';

const TableColumnHeaderLabel = ({ label }: { label: string }) => {
    return <div className="table-header-label">{label}</div>;
};

export default TableColumnHeaderLabel;
